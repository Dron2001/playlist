import { styled, Box } from '@mui/material'

export const Container = styled(Box)(() => ({
  height: '100vh'
}))

export const Body = styled(Box)(() => ({
  width: '100%',
  maxWidth: '980px',
  margin: '35px auto',
  padding: '0 30px'
}))
